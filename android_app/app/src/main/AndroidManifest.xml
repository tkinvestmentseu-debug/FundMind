<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.fundmind" xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="FundMind"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.FundMind">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
