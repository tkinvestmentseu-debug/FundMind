{
  "models": [
    {
      "title": "OpenAI GPT-4o mini",
      "provider": "openai",
      "model": "gpt-4o-mini"
    }
  ],
  "tabAutocompleteModel": "OpenAI GPT-4o mini",
  "rules": [
    "Nigdy nie modyfikuj plików poza ./src, ./app i ./locales.",
    "Nie usuwaj istniejącej logiki. Zmiany mają być backward-compatible.",
    "Każdą większą zmianę rób w osobnym, małym commicie z czytelnym opisem.",
    "Po każdej edycji uruchom npm run typecheck oraz npm test i zatrzymaj, jeśli jest błąd."
  ],
  "allow": ["src/**", "app/**", "locales/**"],
  "agents": [
    {
      "name": "FeatureBot",
      "model": "OpenAI GPT-4o mini",
      "systemPrompt": "Jesteś ostrożnym asystentem programisty. Nie łamiesz reguł z 'rules'. Zmiany robisz minimalne. ZAWSZE proponuj patch jako diff/git patch, a nie przepisywanie plików w całości. Po wprowadzeniu zmian uruchom npm run typecheck i npm test i pokaż tylko wynik. Jeśli testy lub typy padają — cofnij lub popraw minimalnym patchem."
    }
  ]
}
