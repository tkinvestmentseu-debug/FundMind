// Minimal ESLint 9 flat config (ESM) for FundMind
// If you want stricter React/React Native rules later, we can extend this.

// @eslint/js provides the recommended base rules
import js from "@eslint/js";

export default [
  js.configs.recommended,
  {
    ignores: ["backups/"],
  },
];
